# =============================================================================
# ADs Growth System - Prometheus Alert Rules
# =============================================================================
groups:
  - name: stratum-api
    interval: 30s
    rules:
      # High Error Rate
      # stratum_http_request_duration_seconds is emitted by the
      # prometheus-fastapi-instrumentator (app/core/metrics.py); status is
      # grouped ("2xx", "5xx", ...) and handler is the templated route path.
      - alert: HighErrorRate
        expr: |
          sum(rate(stratum_http_request_duration_seconds_count{status="5xx"}[5m]))
          / sum(rate(stratum_http_request_duration_seconds_count[5m])) > 0.05
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "High error rate detected"
          description: "Error rate is {{ $value | humanizePercentage }} over the last 5 minutes"

      # High Latency
      - alert: HighLatency
        expr: |
          histogram_quantile(0.95, sum(rate(stratum_http_request_duration_seconds_bucket[5m])) by (le)) > 0.5
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "High API latency detected"
          description: "95th percentile latency is {{ $value | humanizeDuration }}"

      # API Down
      - alert: APIDown
        expr: up{job="stratum-api"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Stratum API is down"
          description: "The Stratum API has been unreachable for more than 1 minute"

      # Worker Down
      - alert: WorkerDown
        expr: up{job="stratum-worker"} == 0
        for: 2m
        labels:
          severity: warning
        annotations:
          summary: "Celery worker is down"
          description: "The Celery worker has been unreachable for more than 2 minutes"

  - name: stratum-resources
    interval: 30s
    rules:
      # High Memory Usage
      - alert: HighMemoryUsage
        expr: |
          process_resident_memory_bytes{job="stratum-api"}
          / 1024 / 1024 / 1024 > 2
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "High memory usage"
          description: "API memory usage is {{ $value | humanize }}GB"

      # High CPU Usage
      - alert: HighCPUUsage
        expr: |
          rate(process_cpu_seconds_total{job="stratum-api"}[5m]) > 0.8
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "High CPU usage"
          description: "API CPU usage is {{ $value | humanizePercentage }}"

  - name: stratum-database
    interval: 30s
    rules:
      # Database Connection Pool Exhausted
      - alert: DatabaseConnectionPoolExhausted
        expr: |
          db_pool_size{job="stratum-api"} - db_pool_available{job="stratum-api"}
          >= db_pool_size{job="stratum-api"} * 0.9
        for: 2m
        labels:
          severity: warning
        annotations:
          summary: "Database connection pool nearly exhausted"
          description: "{{ $value }} connections in use out of pool"

      # Slow Database Queries
      - alert: SlowDatabaseQueries
        expr: |
          histogram_quantile(0.95, sum(rate(db_query_duration_seconds_bucket[5m])) by (le)) > 1
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "Slow database queries detected"
          description: "95th percentile query time is {{ $value | humanizeDuration }}"

  - name: stratum-trust-layer
    interval: 30s
    rules:
      # Signal Health Degraded
      - alert: SignalHealthDegraded
        expr: |
          avg(stratum_signal_health_score{job="stratum-api"}) < 70
        for: 10m
        labels:
          severity: warning
        annotations:
          summary: "Signal health degraded"
          description: "Average signal health score is {{ $value }}"

      # Autopilot Frozen
      - alert: AutopilotFrozen
        expr: |
          stratum_autopilot_mode{job="stratum-api"} == 0
        for: 30m
        labels:
          severity: warning
        annotations:
          summary: "Autopilot has been frozen"
          description: "Autopilot mode has been frozen for more than 30 minutes"

  - name: stratum-rate-limiting
    interval: 30s
    rules:
      # High Rate Limiting
      - alert: HighRateLimiting
        expr: |
          sum(rate(rate_limit_exceeded_total[5m])) > 10
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "High rate limiting detected"
          description: "{{ $value | humanize }} rate limit hits per second"
