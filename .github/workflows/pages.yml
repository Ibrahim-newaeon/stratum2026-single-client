name: Deploy Docs to GitHub Pages

on:
  push:
    branches: ["main"]
    paths:
      - "backend/docs/**"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Pages
        uses: actions/configure-pages@v6

      # If your docs are already static files (HTML/CSS/JS) inside /backend/docs
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: backend/docs

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
