# =============================================================================
# ADs Growth System - Continuous Integration
# =============================================================================
name: CI

on:
  push:
    branches: [main, develop]
    # Documentation-only changes cannot affect lint, types, tests, or the
    # residue gate (whose own EXCL list already skips *.md, docs/ and
    # backend/docs/). Skipping them keeps ~13 runner-jobs off the clock per
    # docs commit. Safe to filter: main has no branch protection, so no
    # required check can hang waiting on a skipped run.
    paths-ignore:
      - "**.md"
      - "docs/**"
  pull_request:
    branches: [main, develop]
    paths-ignore:
      - "**.md"
      - "docs/**"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: "3.11"
  NODE_VERSION: "22"

jobs:
  # ===========================================================================
  # Removed-Scope Residue Gate (STRAT-SC-001 single-client conversion)
  # ===========================================================================
  # Guards against tenant_id / X-Tenant-ID / Stripe / tenant-namespaced-key
  # residue creeping back into the source tree now that multi-tenancy and
  # billing have been fully removed.
  residue-gate:
    name: Removed-scope residue gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: No tenant/stripe residue in source
        run: |
          set -e
          # Historical artifacts (docs, datasets, audit reports, changelogs) are
          # excluded — they record what USED to exist, not current behavior.
          # Two narrow per-file exclusions (STRAT-SC-001/D3-review handoff),
          # both coordinator-reviewed and pre-ruled — never widen these beyond
          # the exact files below:
          #   - frontend/src/api/client.test.ts: the negative test asserting
          #     X-Tenant-ID is ABSENT necessarily names the header string.
          #   - frontend/src/components/routing/LegacyTenantRedirect.(tsx|test.tsx):
          #     the legacy-bookmark redirect's whole job is parsing the old
          #     /app/${tenantId} path shape to bounce it somewhere valid.
          # Additional narrow exclusions found + documented at E1 time (all
          # tooling/vendor/dead-code, not application source):
          #   - .github/workflows/ci.yml: this step's own detection literals
          #     ('tenant_id', 'X-Tenant-ID', 'stripe') otherwise self-match.
          #   - .claude/hooks/rule-check.sh: a dev-tooling lint hook whose job
          #     is to WARN when tenant_id reappears in Pydantic schemas — it
          #     necessarily contains the string it's guarding against.
          #   - .secrets.baseline: detect-secrets' auto-generated plugin list
          #     always includes "StripeDetector" (a stock plugin), regardless
          #     of whether the app integrates Stripe.
          #   - .scratch: a vendored third-party reference bundle (external
          #     landing-page export, "bombon"), not Stratum application source.
          #   - test.css: a vendored/misplaced WordPress core stylesheet at
          #     repo root; its `.is-style-stripes` table classes false-positive
          #     on the "stripe" substring. Unrelated to payments.
          EXCL=':!*.md :!*.html :!*.txt :!*.xlsx :!*.zip :!*.csv :!datasets :!audit :!superads-dashboard :!docs :!backend/docs :!chat.md :!frontend/src/api/client.test.ts :!frontend/src/components/routing/LegacyTenantRedirect.tsx :!frontend/src/components/routing/LegacyTenantRedirect.test.tsx :!.github/workflows/ci.yml :!.claude/hooks/rule-check.sh :!.secrets.baseline :!.scratch :!test.css'
          if git grep -I -iE 'tenant_id|X-Tenant-ID|stripe' -- . $EXCL; then
            echo '::error::removed-scope residue found'; exit 1; fi
          if git grep -I -nE '\{tenant' -- . $EXCL; then
            echo '::error::tenant-namespaced key builder found'; exit 1; fi

  # ===========================================================================
  # Backend Quality Gates (lint / format / imports / types)
  # ===========================================================================
  # Every gate in the backend jobs runs with continue-on-error and a final
  # aggregator step, so an early failure can't mask the later gates. This is
  # the #343 hardening: the old sequential job died at the first failing step
  # (Black), which hid never-running integration tests, a coverage gate that
  # had never passed, and Bandit findings for months.
  backend-quality:
    name: Backend Quality
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Lint with Ruff
        id: ruff
        continue-on-error: true
        run: ruff check . --output-format=github

      - name: Check formatting with Black
        id: black
        continue-on-error: true
        run: black --check --diff .

      - name: Check import sorting with isort
        id: isort
        continue-on-error: true
        run: isort --check-only --diff .

      - name: Type check with mypy
        id: mypy
        continue-on-error: true
        run: mypy app --ignore-missing-imports --no-error-summary

      - name: Aggregate quality gates
        env:
          GATE_RUFF: ${{ steps.ruff.outcome }}
          GATE_BLACK: ${{ steps.black.outcome }}
          GATE_ISORT: ${{ steps.isort.outcome }}
          GATE_MYPY: ${{ steps.mypy.outcome }}
        run: |
          failed=0
          for gate in "Ruff:$GATE_RUFF" "Black:$GATE_BLACK" \
                      "isort:$GATE_ISORT" "mypy:$GATE_MYPY"; do
            name="${gate%%:*}"; outcome="${gate##*:}"
            echo "$name: $outcome"
            if [ "$outcome" != "success" ]; then
              echo "::error::Quality gate failed: $name"
              failed=1
            fi
          done
          exit $failed

  # ===========================================================================
  # Backend Tests & Coverage
  # ===========================================================================
  backend-tests:
    name: Backend Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    env:
      DATABASE_URL: postgresql+asyncpg://test:test@localhost:5432/stratum_test
      DATABASE_URL_SYNC: postgresql://test:test@localhost:5432/stratum_test
      # The integration conftest reads TEST_DATABASE_URL[_SYNC] (falling back
      # to a local "stratum" default) — point it at the CI Postgres service
      # so it doesn't try to auth as a non-existent "stratum" role.
      TEST_DATABASE_URL: postgresql+asyncpg://test:test@localhost:5432/stratum_test
      TEST_DATABASE_URL_SYNC: postgresql://test:test@localhost:5432/stratum_test
      REDIS_URL: redis://localhost:6379/0
      SECRET_KEY: test-secret-key-that-is-at-least-32-chars-long
      JWT_SECRET_KEY: test-jwt-secret-that-is-at-least-32-chars
      PII_ENCRYPTION_KEY: dGVzdC1lbmNyeXB0aW9uLWtleS0zMmJ5dGVz
      APP_ENV: test
      PYTHONPATH: .

    services:
      postgres:
        # pgvector image: the integration harness runs the real Alembic
        # chain, which includes migration 049 (CREATE EXTENSION vector).
        # Plain postgres:16 lacks it. Same as the load-tests job.
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: stratum_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run unit tests
        id: unit
        continue-on-error: true
        run: |
          coverage erase
          pytest tests/unit -v --tb=short -m "unit or not integration" \
            --junitxml=unit-junit.xml \
            --cov=app --cov-config=.coveragerc --cov-append --cov-fail-under=0

      - name: Run integration tests
        id: integration
        continue-on-error: true
        run: |
          # Integration tests run on a single session-scoped event loop: the
          # FastAPI test app uses Starlette BaseHTTPMiddleware (AuthContextMiddleware),
          # which under pytest-asyncio's default per-test loop yields
          # "Future attached to a different loop" / "Event loop is closed".
          # Scoped here (not in pytest.ini) so the unit suite keeps its loops.
          pytest tests/integration -v --tb=short -m "integration" \
            -o asyncio_default_test_loop_scope=session \
            -o asyncio_default_fixture_loop_scope=session \
            --junitxml=integration-junit.xml \
            --cov=app --cov-config=.coveragerc --cov-append --cov-fail-under=0

      - name: Assert suites actually ran (vacuous-pass guard)
        id: rancount
        continue-on-error: true
        run: |
          # A suite that collects 0 tests or skips nearly everything (e.g. a
          # broken DB connection turning into skips) must fail loudly, not
          # pass vacuously. Floors sit well under the current counts
          # (3561 unit / 706 integration at time of writing) but far above
          # any plausible "the harness silently broke" number.
          python - <<'EOF'
          import sys
          import xml.etree.ElementTree as ET

          FLOORS = {"unit-junit.xml": 3000, "integration-junit.xml": 600}
          failed = False
          for path, floor in FLOORS.items():
              try:
                  root = ET.parse(path).getroot()
              except (OSError, ET.ParseError) as exc:
                  print(f"::error::{path}: unreadable junit report ({exc})")
                  failed = True
                  continue
              suites = [root] if root.tag == "testsuite" else list(root)
              tests = sum(int(s.get("tests", 0)) for s in suites)
              skipped = sum(int(s.get("skipped", 0)) for s in suites)
              ran = tests - skipped
              print(f"{path}: collected={tests} skipped={skipped} ran={ran} floor={floor}")
              if ran < floor:
                  print(f"::error::{path}: only {ran} tests ran, floor is {floor}")
                  failed = True
          sys.exit(1 if failed else 0)
          EOF

      - name: Coverage report (ratchet gate)
        id: coverage
        continue-on-error: true
        run: |
          # Combined unit + integration coverage: both test steps above ran with
          # --cov-append into a single .coverage file (unit and integration each
          # exercise different layers — endpoints/services are driven by the
          # integration suite). Emit the combined report + xml and enforce the
          # .coveragerc fail_under (the #342 ratchet — only ever raise it) on
          # the COMBINED total.
          coverage xml
          coverage report --show-missing

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v7
        with:
          file: backend/coverage.xml
          flags: backend
          fail_ci_if_error: false

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: backend-coverage
          path: backend/htmlcov
          retention-days: 7

      - name: Aggregate test gates
        env:
          GATE_UNIT: ${{ steps.unit.outcome }}
          GATE_INTEGRATION: ${{ steps.integration.outcome }}
          GATE_RANCOUNT: ${{ steps.rancount.outcome }}
          GATE_COVERAGE: ${{ steps.coverage.outcome }}
        run: |
          failed=0
          for gate in "Unit tests:$GATE_UNIT" \
                      "Integration tests:$GATE_INTEGRATION" \
                      "Ran-count guard:$GATE_RANCOUNT" \
                      "Coverage ratchet:$GATE_COVERAGE"; do
            name="${gate%%:*}"; outcome="${gate##*:}"
            echo "$name: $outcome"
            if [ "$outcome" != "success" ]; then
              echo "::error::Test gate failed: $name"
              failed=1
            fi
          done
          exit $failed

  # ===========================================================================
  # Backend Security Gates (Bandit / pip-audit)
  # ===========================================================================
  backend-security:
    name: Backend Security
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Security scan with Bandit
        id: bandit
        continue-on-error: true
        run: |
          pip install bandit
          bandit -r app/ -ll -ii

      - name: Dependency audit (pip-audit)
        id: pipaudit
        continue-on-error: true
        run: |
          # Upgrade packaging toolchain first — the setup-python bundled
          # setuptools carries known advisories (e.g. PYSEC-2026-3447), same
          # rationale as the backend Dockerfile's pip/setuptools/wheel upgrade.
          pip install --upgrade pip setuptools wheel
          pip install pip-audit
          pip-audit --strict --desc on

      - name: Aggregate security gates
        env:
          GATE_BANDIT: ${{ steps.bandit.outcome }}
          GATE_PIPAUDIT: ${{ steps.pipaudit.outcome }}
        run: |
          failed=0
          for gate in "Bandit:$GATE_BANDIT" "pip-audit:$GATE_PIPAUDIT"; do
            name="${gate%%:*}"; outcome="${gate##*:}"
            echo "$name: $outcome"
            if [ "$outcome" != "success" ]; then
              echo "::error::Security gate failed: $name"
              failed=1
            fi
          done
          exit $failed

  # ===========================================================================
  # Frontend Tests & Build
  # ===========================================================================
  frontend:
    name: Frontend CI
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Dependency audit (npm)
        # Was a bare `npm audit --audit-level=high --omit=dev`. npm audit has
        # no native ignore, so one unfixable advisory left only two options:
        # delete the gate or append `|| true` — both stop checking everything
        # else. check-audit.mjs keeps it failing on every high/critical
        # advisory EXCEPT those documented in frontend/.audit-allowlist.json
        # with a reason and a removal condition, and reports stale entries.
        run: node scripts/check-audit.mjs

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npx tsc --noEmit

      - name: Run unit tests with coverage
        run: npm run test:coverage -- --reporter=verbose

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v7
        with:
          file: frontend/coverage/lcov.info
          flags: frontend
          fail_ci_if_error: false

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: frontend-coverage
          path: frontend/coverage
          retention-days: 7

      - name: Build
        run: npm run build
        env:
          VITE_API_URL: http://localhost:8000/api/v1

      - name: Upload build artifact
        uses: actions/upload-artifact@v7
        with:
          name: frontend-build
          path: frontend/dist
          retention-days: 7

  # ===========================================================================
  # E2E Tests (runs after frontend build)
  # ===========================================================================
  e2e:
    name: E2E Tests
    runs-on: ubuntu-latest
    needs: [frontend]
    if: github.event_name == 'pull_request'
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run E2E tests
        run: npm run test:e2e -- --project=chromium

      - name: Upload E2E test results
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: playwright-report
          path: frontend/playwright-report
          retention-days: 7

  # ===========================================================================
  # Security Scan
  # ===========================================================================
  security:
    name: Security Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Run Trivy filesystem scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: "fs"
          scan-ref: "."
          format: "sarif"
          output: "trivy-fs-results.sarif"
          severity: "CRITICAL,HIGH"
          ignore-unfixed: true
          # Passed explicitly rather than relying on Trivy discovering
          # .trivyignore in the working directory — the action runs Trivy in
          # its own context, so cwd auto-discovery is not guaranteed.
          trivyignores: ".trivyignore"
          # Required whenever format is sarif. Without it the action logs
          # "Building SARIF report with all severities" and DISCARDS the
          # severity filter above — so `exit-code: 1` then fails the job on any
          # finding at any severity, including LOW and MEDIUM.
          limit-severities-for-sarif: true
          exit-code: "1"

      - name: Build Docker image for scanning
        run: docker build -t stratum-api:scan ./backend

      - name: Trivy container image scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: "stratum-api:scan"
          format: "sarif"
          output: "trivy-image-results.sarif"
          severity: "CRITICAL,HIGH"
          ignore-unfixed: true
          # This scan is the one that actually needs the ignore file. The two
          # suppressed advisories (pip-vendored msgpack, ensurepip's bundled
          # setuptools) are present in the linux/amd64 image runners build and
          # absent from a linux/arm64 build — so a local arm64 scan passes
          # whether or not the ignore file is applied, and cannot confirm it is.
          trivyignores: ".trivyignore"
          # Required whenever format is sarif. Without it the action logs
          # "Building SARIF report with all severities" and DISCARDS the
          # severity filter above — so `exit-code: 1` then fails the job on any
          # finding at any severity, including LOW and MEDIUM.
          limit-severities-for-sarif: true
          exit-code: "1"

      - name: Upload Trivy scan results
        uses: github/codeql-action/upload-sarif@v4.37.9
        if: always()
        with:
          sarif_file: "trivy-fs-results.sarif"
        continue-on-error: true  # Requires GitHub Advanced Security; not all repos have code scanning

  # ===========================================================================
  # Secret Scanning
  # ===========================================================================
  secrets:
    name: Secret Scanning
    runs-on: ubuntu-latest
    # gitleaks-action crashes on Dependabot-actor PRs (read-only token breaks
    # its PR-comment API calls). Dependency bumps can't introduce repo secrets;
    # gitleaks still runs on pushes and human PRs.
    if: github.actor != 'dependabot[bot]'

    # On pull_request, gitleaks-action calls GET /repos/{o}/{r}/pulls/{n}/commits
    # to work out the commit range to scan. The repository default workflow
    # permission is `read`, which covers contents but NOT pull-requests, so that
    # call returned 403 and the action died:
    #
    #   event type: pull_request
    #   RequestError [HttpError]: Resource not accessible by integration
    #     at async Object.ScanPullRequest (.../dist/index.js:127929:17)
    #
    # It passed on main only because push events never hit that endpoint — so
    # `secrets`, a Release Gate requirement, was red on every human PR while
    # main looked clean.
    permissions:
      contents: read
      pull-requests: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Run gitleaks
        uses: gitleaks/gitleaks-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ===========================================================================
  # Release Gate (all required checks must pass)
  # ===========================================================================
  gate:
    name: Release Gate
    runs-on: ubuntu-latest
    needs: [residue-gate, backend-quality, backend-tests, backend-security, frontend, security, secrets]
    if: always()

    steps:
      - name: Check all required jobs passed
        run: |
          echo "Residue Gate: ${{ needs.residue-gate.result }}"
          echo "Backend Quality: ${{ needs.backend-quality.result }}"
          echo "Backend Tests: ${{ needs.backend-tests.result }}"
          echo "Backend Security: ${{ needs.backend-security.result }}"
          echo "Frontend: ${{ needs.frontend.result }}"
          echo "Security: ${{ needs.security.result }}"
          echo "Secrets: ${{ needs.secrets.result }}"
          if [[ "${{ needs.residue-gate.result }}" != "success" ]] || \
             [[ "${{ needs.backend-quality.result }}" != "success" ]] || \
             [[ "${{ needs.backend-tests.result }}" != "success" ]] || \
             [[ "${{ needs.backend-security.result }}" != "success" ]] || \
             [[ "${{ needs.frontend.result }}" != "success" ]] || \
             [[ "${{ needs.security.result }}" != "success" ]] || \
             [[ "${{ needs.secrets.result }}" != "success" && "${{ needs.secrets.result }}" != "skipped" ]]; then
            echo "::error::One or more required jobs failed — merge blocked"
            exit 1
          fi
          echo "All release gate checks passed"

  # ===========================================================================
  # Load Tests (on main branch merges only)
  # ===========================================================================
  load-tests:
    name: Load Tests
    runs-on: ubuntu-latest
    needs: [backend-tests, frontend]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    services:
      postgres:
        # pgvector image: load-tests runs `alembic upgrade head`, which includes
        # migration 049 (CREATE EXTENSION vector). Plain postgres:16 lacks it.
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: stratum_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"
          cache-dependency-path: backend/requirements.txt

      - name: Install backend dependencies
        working-directory: backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Start API server
        working-directory: backend
        env:
          DATABASE_URL: postgresql+asyncpg://test:test@localhost:5432/stratum_test
          DATABASE_URL_SYNC: postgresql://test:test@localhost:5432/stratum_test
          REDIS_URL: redis://localhost:6379/0
          SECRET_KEY: test-secret-key-for-load-tests
          JWT_SECRET_KEY: test-jwt-secret-for-load-tests
          PII_ENCRYPTION_KEY: dGVzdC1lbmNyeXB0aW9uLWtleS0zMmJ5dGVz
          APP_ENV: test
          # Skip startup ML auto-training so uvicorn starts accepting
          # connections immediately; otherwise it trains from sample data during
          # lifespan startup and /health is refused while the k6 smoke check
          # runs (the autopilot endpoints under test don't need ML models).
          ML_AUTO_TRAIN: "false"
          # The lifespan superadmin seeder imports scripts/seed_superadmin, which
          # sys.exit(1)s at import when these are unset (SystemExit isn't caught
          # by the lifespan's except Exception) — that aborts startup once ML
          # training no longer blocks it first. Provide test values.
          SUPERADMIN_EMAIL: admin@stratum.test
          SUPERADMIN_PASSWORD: test_superadmin_password_change_me
        run: |
          # Widen alembic_version.version_num to VARCHAR(128) first: some
          # revision ids (e.g. 041_add_superadmin_to_userrole_enum, 35 chars)
          # exceed Alembic's default VARCHAR(32), which otherwise fails
          # `alembic upgrade head`. start.sh does this in the app image; the
          # load-test job runs alembic directly, so do it here too.
          python fix_alembic_version.py
          # Run migrations
          alembic upgrade head
          # Seed load test user
          python scripts/seed_load_test_user.py
          # Start server in background
          uvicorn app.main:app --host 0.0.0.0 --port 8000 &
          sleep 10  # Wait for server to start

      - name: Run load test smoke tests
        run: |
          docker run --rm --network host \
            -e BASE_URL=http://localhost:8000 \
            -e SCENARIO=smoke \
            -i grafana/k6 run - < tests/load/autopilot-enforcement-load-test.js

      - name: Upload load test results
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: load-test-results
          path: tests/load/*.json
          retention-days: 7
