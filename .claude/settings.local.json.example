{
  "_comment": "Copy to settings.local.json for personal overrides. This file is gitignored; never place credentials in the example.",
  "permissions": {
    "allow": [
      "Bash(psql:*)",
      "Bash(redis-cli:*)"
    ]
  }
}
